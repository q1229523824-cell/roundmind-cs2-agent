# RoundMind CS2 Agent：后续优化计划与目标架构

## 1. 项目最终要解决什么问题

RoundMind 的目标不是做一个“上传 Demo 后生成一段好听文字”的聊天机器人，而是做一个**可验证、可追溯、能长期理解玩家的 CS2 复盘教练**：

1. 正确读取 Demo，重建玩家当时真正面对的局势；
2. 判断接战、站位、补枪、道具和经济决策是否合理；
3. 用具体回合和数据解释判断依据；
4. 跨多场识别重复习惯，而不是被单场波动误导；
5. 最后才让大模型把结构化结论组织成个性化、能继续追问的教练对话。

因此项目的核心护城河应当是：

```text
Demo 解析准确度 × 局势建模能力 × 决策评测可信度 × 长期个性化
```

大模型负责表达、追问和训练计划，不负责凭空计算比赛事实。

## 2. 当前已经具备的能力

| 层级 | 当前能力 | 当前边界 |
| --- | --- | --- |
| 数据层 | 解析 `.dem`，提取玩家、回合、击杀、伤害、道具、经济和交火 | 游戏更新后需要兼容性回归；部分空间关系仍是代理指标 |
| 质量层 | 数据质量门禁、重复事件检查、SteamID、覆盖率检查 | 缺少更大规模的真实 Demo 金标准 |
| 分析层 | 首轮交火、补枪、残局、经济、道具、接战风险和候选动作 | 权重主要来自规则与少量工程场景，尚未经过教练级标注校准 |
| 知识层 | Dust2 本地知识库、关键词/可选向量检索、个人成功案例 | 地图覆盖有限；知识质量比知识数量更重要 |
| 个性化 | 多场玩家画像、角色倾向、趋势和训练目标 | 样本较少时画像容易不稳定，需要时间衰减和最小样本门槛 |
| Agent 层 | LangGraph 多智能体协作、证据白名单、离线回答、可选 DeepSeek | 还应加强失败恢复、动态预算和回答质量评测 |
| 产品层 | 500 MB Demo、选人、进度、历史、画像、质量报告、连续对话 | 正式账号体系和持久化需要启用 PostgreSQL/JWT |
| 工程层 | FastAPI、React、Docker、Render、Sites、测试、可选 PostgreSQL/Redis/Celery/R2 | 免费部署适合演示，不适合高并发大文件生产流量 |

## 3. 目标技术架构

```text
React / TypeScript Web
  ├─ 上传与任务进度
  ├─ 历史比赛 / 玩家画像 / 训练计划
  └─ 证据化教练对话
            │
            ▼
FastAPI API Gateway
  ├─ JWT、所有权、限流、请求追踪
  ├─ Demo Job API
  └─ Match / Profile / Coach API
            │
     ┌──────┴────────┐
     ▼               ▼
Object Storage     Redis Queue
原始 Demo 临时存储   Celery Worker
                     │
                     ▼
              demoparser2 Parser
                     │
                     ▼
          Canonical Match Event Schema
                     │
       ┌─────────────┼──────────────┐
       ▼             ▼              ▼
  质量门禁       局势状态重建      玩家特征聚合
       │             │              │
       └─────────────┼──────────────┘
                     ▼
        决策引擎 + 候选动作评分
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
    PostgreSQL/pgvector      评测与标注集
    比赛、画像、知识、版本     准确率、校准度、覆盖率
          │                     │
          └──────────┬──────────┘
                     ▼
              LangGraph Multi-Agent Team
  事实工具 → 画像工具 → 知识检索 → 建议生成 → 证据审核
                     │
                     ▼
             离线模板 / 可选大模型表达
```

## 4. Agent 应拥有的工具

Agent 不应直接读取整份 Demo，而应调用范围明确的结构化工具：

| 工具 | 输入 | 输出 | 作用 |
| --- | --- | --- | --- |
| `get_match_summary` | match_id | KDA、ADR、KAST、比分 | 建立比赛概览 |
| `inspect_round` | match_id、round | 回合时间线和状态快照 | 回答“这一回合为什么输” |
| `analyze_contacts` | player、筛选条件 | 完整交火与先手关系 | 分析对枪和脱离选择 |
| `compare_candidate_actions` | situation_id | 候选动作、风险、成立前提 | 输出决策卡 |
| `find_personal_baseline` | situation_id | 相似成功/失败回合 | 给出个人化反例 |
| `get_player_profile` | steamid、地图、时间窗 | 角色、趋势、重复习惯 | 支持长期个性化 |
| `retrieve_tactical_knowledge` | 地图、点位、阵营、问题 | 有来源的战术原则 | RAG，不替代事实计算 |
| `build_training_plan` | 画像、目标、可用时间 | 可量化训练安排 | 把诊断转成行动 |
| `verify_claims` | 草稿、证据 ID | 支持/不支持的结论 | 阻止模型编造 |

每个工具都应限制输入范围、输出稳定 JSON，并记录工具版本和证据 ID。Agent 只能引用工具实际返回的回合与知识来源。

## 5. 优化优先级

### P0：先证明数据是对的

这是当前最高优先级。如果事实错了，RAG 和大模型只会更有说服力地输出错误答案。

工作项：

- 建立 10—20 份不同版本、地图、加时和特殊情况的 Demo 回归集；
- 为每份 Demo 保存可人工核对的比分、玩家、K/D/A、首杀、首死和关键回合金标准；
- 输出解析器版本、Demo 协议版本和功能兼容矩阵；
- 给装备价值、闪白、队友闪、重复伤害和首轮交火增加专项回归用例；
- 游戏更新后自动跑旧 Demo 与新 Demo，对比字段覆盖率和异常变化；
- 将“解析正确率”和“无法解析率”加入版本发布门禁。

验收指标：

- 比分、回合数、K/D/A 与游戏结算页一致率达到 100%；
- 首杀/首死人工抽检一致率不低于 98%；
- 重复事件和队友闪误计率低于 1%；
- 不支持的新版本 Demo 必须明确失败，不能生成空报告。

### P1：提高局势判断和候选动作准确度

工作项：

- 把每次交火统一为“接触前状态 → 动作 → 结果”的事件模型；
- 增加人数、血量、武器、经济、时间、炸弹、队友视角、烟火遮挡和可补枪条件；
- 区分信息获取、主动突破、补枪、守包、回防、残局和保枪场景；
- 候选动作先由规则生成，再分别计算风险和成立前提；
- 对风险分做概率校准，避免 80 分只是看起来精确；
- 用相似成功回合提供反事实依据：“同类情况下你怎样做成功过”。

验收指标：

- 真实人工标注集达到至少 100 张决策卡；
- 系统首选动作与人工标注一致率达到 75% 以上；
- Top-2 候选动作覆盖人工选择达到 90% 以上；
- 高置信建议的一致率必须明显高于低置信建议；
- 不确定场景允许回答“证据不足”，而不是强行下结论。

### P2：让建议真正个性化

工作项：

- 玩家画像按地图、阵营、武器角色、点位和最近时间窗分层；
- 使用 SteamID 作为身份主键，昵称只用于显示；
- 区分“本场异常”“正在形成的模式”“稳定习惯”；
- 对旧比赛做时间衰减，避免半年前的问题永久影响画像；
- 每次只推荐 1—2 个最高收益训练目标，并追踪后续变化；
- 增加建议去重，避免多场报告反复出现同一句话；
- 让大模型根据玩家角色、可用训练时间和历史目标调整表达与计划。

验收指标：

- 建议中每条核心判断都能指向具体回合或跨场指标；
- 连续三场不重复输出无变化的通用建议；
- 训练目标包含基线、目标值、期限和复测方法；
- 用户可以追问“为什么”“给我训练计划”“我有改善吗”。

### P3：把网页做成可持续使用的产品

工作项：

- 正式启用 PostgreSQL、JWT、用户注册和数据所有权隔离；
- 历史页支持地图、日期、玩家、质量状态筛选；
- 画像页显示趋势曲线、优势、问题和正在执行的训练目标；
- Demo 任务支持断点提示、失败重试、取消和后台通知；
- 提供数据导出、删除账号、删除比赛和隐私说明；
- 增加“本地解析模式”，让大 Demo 留在用户电脑，只上传结构化结果；
- 为移动端做只读报告与连续追问体验。

验收指标：

- 新用户不看说明也能完成上传、选人、查看报告；
- 失败状态都给出可执行的下一步；
- 任意用户无法读取其他用户的比赛、任务和画像；
- 500 MB Demo 不经过前端 Serverless 中转。

### P4：工程化、成本与可运营性

工作项：

- PostgreSQL 保存用户、比赛、画像和版本；
- R2/S3 临时保存 Demo，解析完成后按策略删除；
- Redis + Celery 处理长任务、重试和跨实例状态；
- 增加解析耗时分位数、成功率、队列深度、错误类型和模型费用监控；
- 对 Demo 哈希去重，避免同一文件重复解析；
- 缓存相同比赛的分析结果和画像聚合结果；
- 建立数据库备份、迁移、回滚和数据保留策略；
- 大模型设置调用预算、超时、熔断和离线回退。

验收指标：

- API 重启不丢比赛和用户数据；
- Worker 重启后任务可以继续或安全重试；
- 能回答一次 Demo 从上传到报告的耗时和失败原因；
- 未配置或超出模型预算时，确定性报告仍然可用。

## 6. 推荐的六个迭代版本

| 版本 | 主题 | 可交付结果 |
| --- | --- | --- |
| V1 | Demo 金标准 | 已完成框架：匿名草稿、人工核对 Schema、字段差异、批量准确率报告与解析器版本指纹 |
| V2 | 决策标注闭环 | 前端标注、标注导出、Top-1/Top-2 与置信度指标 |
| V3 | 局势引擎 2.0 | 场景分类、统一状态向量、可校准风险分 |
| V4 | 个性化教练 | 时间衰减画像、目标追踪、建议去重、训练计划 |
| V5 | 正式账号产品 | PostgreSQL、JWT、历史筛选、数据删除与导出 |
| V6 | 异步生产管线 | R2、Redis/Celery、监控、缓存和故障恢复 |

每个版本都必须包含：代码、自动化测试、至少一项量化指标、README/设计文档和可复现演示。

## 7. 数据应该怎样继续收集

多给 Demo 有帮助，但不是直接“投喂给大模型”。Demo 应进入以下闭环：

```text
新 Demo
  → 自动解析
  → 质量门禁
  → 与结算数据核对
  → 抽取代表决策卡
  → 人工选择更合理动作
  → 更新评测集/评分参数
  → 回归测试
```

最有价值的数据不是数量最多的数据，而是能够覆盖边界的数据：不同地图、T/CT、不同武器、优势/劣势人数、残局、保枪、加时、烟火遮挡、队友远近以及游戏版本更新前后。

建议保存三类数据，彼此分离：

- 原始 Demo：只用于解析，设置删除周期；
- 匿名结构化比赛事实：用于画像和回归；
- 人工决策标注：用于评测和校准，不把 Agent 自己的输出当作真值。

## 8. RAG、大模型与微调的正确顺序

1. **规则与程序计算事实**：比分、交火、经济、位置和时间线；
2. **RAG 提供知识**：地图战术、训练原则、版本规则和来源；
3. **玩家画像提供上下文**：角色、长期习惯、当前目标和变化趋势；
4. **大模型组织回答**：解释证据、适配语气、生成计划并处理追问；
5. **评测器审核回答**：检查引用、事实一致性、可执行性和重复度；
6. 只有积累足够高质量标注后，再评估是否微调模型。

当前不建议优先微调。现阶段误差更可能来自 Demo 事实、局势特征和评分规则，而不是模型不会说人话。

## 9. 下一步立即执行的任务

金标准 Schema、匿名摘要、差异检测、批量回归、CI 与网页准确率门禁已经完成。公开网页也已提供云端/本地解析切换：本地桥接只监听 `127.0.0.1`，300–500 MB Demo 不经过 Render；Windows x64 免 Python 压缩包可由 GitHub Actions 自动构建并在版本标签发布。下一步用 verified Demo 制作真实决策卡标注集，并把云端任务升级到用户隔离的持久队列。用户暂时只需核对比分、K/D/A、首杀、首死和异常回合。

## 10. 简历与面试的核心表达

项目可以概括为：

> 设计并实现面向 CS2 Demo 的证据化复盘 Agent，使用 demoparser2 将大文件转为结构化比赛事件，通过 FastAPI 异步任务、LangGraph 工具编排、质量门禁、接战决策评分、RAG 和跨场玩家画像生成可追溯建议；以人工标注集评测候选动作准确率，并提供 React 前端、JWT 数据隔离及低成本云部署。

面试时重点讲清三个取舍：为什么不把 Demo 直接交给大模型、为什么先做质量门禁、为什么把“事实计算”和“语言生成”分开。它们比简单罗列框架更能体现 Agent 工程能力。
