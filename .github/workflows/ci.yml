name: RoundMind CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: chapter07_cs2_coach/requirements.txt
      - name: Install backend dependencies
        run: python -m pip install -r chapter07_cs2_coach/requirements.txt
      - name: Run backend and gold-standard tests
        run: python -m unittest discover -s tests -v
      - name: Verify gold-standard CLI contract
        run: python -m chapter07_cs2_coach.gold_cli --help

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm test
